<% content_for :title, "Edit Arrest" %>
<div class="page-header">
  <h1>Edit Arrest</h1>
  <%= link_to "← Back", arrest_path(@arrest), class: "btn btn-secondary" %>
</div>
<div class="form-card">
  <%= render "form", arrest: @arrest %>
</div>
