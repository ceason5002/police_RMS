<% content_for :title, "New Arrest" %>
<div class="page-header">
  <h1>New Arrest</h1>
  <%= link_to "← Back", arrests_path, class: "btn btn-secondary" %>
</div>
<div class="form-card">
  <%= render "form", arrest: @arrest %>
</div>
