<% content_for :title, "Edit Evidence" %>
<div class="page-header">
  <h1>Edit Evidence</h1>
  <%= link_to "← Back", evidence_path(@evidence), class: "btn btn-secondary" %>
</div>
<div class="form-card">
  <%= render "form", evidence: @evidence %>
</div>
